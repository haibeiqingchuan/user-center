spring:
  application:
    name: user-center
  # DataSource Config
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/shanhai
    username: root
    password: shine

server:
  port: 8080

mybatis-plus:
  configuration:
    #关闭数据库自动驼峰转换
    map-underscore-to-camel-case: false
